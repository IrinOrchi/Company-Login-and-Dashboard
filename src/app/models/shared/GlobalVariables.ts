interface GlobalVariables {
    shouldShowHeader: boolean;
    shouldShowFooter: boolean;
}

const defaultGlobalVariables: GlobalVariables = {
    shouldShowHeader: true,
    shouldShowFooter: true
};
